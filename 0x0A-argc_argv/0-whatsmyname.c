#include <stdio.h>
#include "main.h"
/**
 * main - prints program name
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
int i = 0;
printf("%s\n", argv[i]);
return (0);
}
