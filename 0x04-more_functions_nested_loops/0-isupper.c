#include "main.h"
/**
 * _isupper - prints uppercase letters
 * @c: char to check
 * Return: 1 and 0 depending on condition
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
