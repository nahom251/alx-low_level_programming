#include "main.h"
/**
 * _islower - checks if character is in lowercase
 * @c: a character for argument
 * Return: 0 and 1 depending con condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
